[logging]
level = "debug"
filename = "logs/mixer.log"

[api]
port = 8090
tls_cert_file = "../certs/server.crt"
tls_key_file = "../certs/server.key"

[snake]
port = 4223
url = "nats://localhost:4223"
tls_cert_file = "../certs/server.crt"
tls_key_file = "../certs/server.key"
root_ca_file = "../certs/ca.crt"

[store]
dir = "./data"
database_file = "fluxrig_test.duckdb"
cluster_key_file = "cluster.key"
